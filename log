if [ "$1" == "" ]; then

#current branch
currBranch=`git rev-parse --abbrev-ref HEAD`

git log --pretty=format:"%h - %an, %ar : %s" --graph "$currBranch"

else

git log --pretty=format:"%h - %an, %ar : %s" --graph --max-count=$1 "$currBranch"

fi

